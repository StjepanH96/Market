import { NavigationBar } from './NavigationBar';
import {
  HeaderContainer,
} from '@/styled-components';

export const Header = () => {
  return (

      <HeaderContainer>
        <NavigationBar />
      </HeaderContainer>
  
  );
};
